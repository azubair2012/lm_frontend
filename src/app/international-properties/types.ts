export type PropertyCTA = { label: string; href: string };

export type PropertyData = {
  title: string;
  blurb: string;
  cardDescription: string;
  image: string;
  images: string[];
  modalDescription: string[];
  ctas: PropertyCTA[];
};

